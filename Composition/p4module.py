
from parser import parser_composition

class load_p4module:
	#there is a need to include registers
	#but im too lazy now
	__init__(self, extension, host):
	
	    parser = parser_composition(g)
        parser.scan_control_block()


	def parallel_composition:
		print("ERRO")

		with open('your_file.txt', 'w') as f:
            for item in self.apply_block:
                f.write("%s" % item)


